# logisim.py
This python code automatically generates `.circ`-files (i.e., logic circuits) for the logic simulator *logisim-evolution*. More specifically, this python code is used to generate the `kone` logic circuit, that resembles the whole `kone` cpu architecture, including:
1. __CPU__
2. __Keyboard__
3. __Display__
